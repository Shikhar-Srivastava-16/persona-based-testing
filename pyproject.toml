[project]
name = "persona-based-testing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "langchain>=1.3.7",
    "langgraph>=1.2.4",
]
